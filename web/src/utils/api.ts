// TODO(pass 2): replace with axios instance pre-configured with baseURL +
// auth interceptor. Stays empty this pass — components do not import from here.

export const apiClient = {
  // Placeholder for axios instance.
};
